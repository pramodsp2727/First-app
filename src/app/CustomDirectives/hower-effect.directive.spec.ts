import { HowerEffectDirective } from './hower-effect.directive';

describe('HowerEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new HowerEffectDirective();
    expect(directive).toBeTruthy();
  });
});
